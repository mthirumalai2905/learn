const content = "This is a simple example. Example code can be simple.";
const searchQuery = "example";

const highlightedContent = highlightSearchQuery(content, searchQuery);

console.log(highlightedContent);